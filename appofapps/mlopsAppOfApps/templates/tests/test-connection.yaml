apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mlopsAppOfApps.fullname" . }}-test-connection"
  labels:
    {{- include "mlopsAppOfApps.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mlopsAppOfApps.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
