apiVersion: v1
kind: Service
metadata:
  name: {{ include "mlopsAppOfApps.fullname" . }}
  labels:
    {{- include "mlopsAppOfApps.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mlopsAppOfApps.selectorLabels" . | nindent 4 }}
